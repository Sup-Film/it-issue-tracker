# Root .env.example for dev docker-compose (database credentials)
# Copy to .env and edit values before running docker-compose up
# Do NOT commit real secrets.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=it_issue_tracker

# Optional convenience: a DATABASE_URL pointing to the containerized DB
# DATABASE_URL="postgresql://postgres:changeme@db:5432/it_issue_tracker?schema=public"
