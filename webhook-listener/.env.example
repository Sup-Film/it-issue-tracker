# Copy this file to .env and fill values before running the webhook listener.
# Do NOT commit real secrets. Keep .env in .gitignore.

# Shared secret used to verify incoming webhook signatures
WEBHOOK_SECRET=<replace-with-strong-random-secret>

# Port the listener will bind to (container/service uses 4000 by default)
SERVER_PORT=4000
